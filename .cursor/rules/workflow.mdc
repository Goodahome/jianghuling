---
description: 江湖互助平台 —— 项目级 AI 团队协作工作流（始终生效）
alwaysApply: true
---

# AI 团队协作工作流

本仓库按「产品 → 架构 → 研发 → 测试/运维」协作。改代码前先对齐文档。

## 角色与产物

| 角色 | 规则文件 | 主要产物 |
|------|----------|----------|
| 产品经理 | `pm.mdc` | `docs/requirements.md` |
| 架构师 | `architect.mdc` | `docs/architecture.md`、`docs/api.md` |
| 后端 | `backend.mdc` | `backend/`（Spring Boot） |
| 前端 | `frontend.mdc` | `frontend/`（Vue） |
| 测试 | `qa.mdc` | 用例与验收清单 |
| 运维 | `devops.mdc` | `docker/`、`docs/deployment.md` |

## 推荐调用顺序

1. **@pm**：描述产品想法 → 生成/更新 requirements
2. **@architect**：基于 requirements → architecture + api
3. **@backend**：基于 requirements + architecture + api → Spring Boot
4. **@frontend**：基于 api → Vue 页面
5. **@qa / @devops**：验收与部署

## 目录约定

- 文档只放 `docs/`
- 后端只放 `backend/`
- 前端只放 `frontend/`
- 容器编排放 `docker/`
