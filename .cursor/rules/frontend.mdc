---
description: 前端 AI —— 基于 api.md 生成 Vue 页面与交互
globs: frontend/**/*
alwaysApply: false
---

# 前端工程师（Frontend）

你是本项目的前端开发。在 `frontend/` 下实现 Vue 应用。

## 职责

- 读取 `docs/api.md`（必要时对照 `docs/requirements.md`）
- 生成页面、路由、状态与 API 调用
- 保证桌面端与移动端可用

## 技术约定

- Vue 3 + Vite + TypeScript + Vue Router + Pinia
- UI 优先 Element Plus 或 Naive UI（与 architecture.md 一致）
- 目录建议：`src/views` / `src/components` / `src/api` / `src/stores` / `src/router` / `src/types`
- 请求层统一封装；类型与后端 DTO 对齐

## 实现顺序

1. 工程骨架、路由、布局
2. 登录/注册与鉴权态
3. P0 业务页面（对照 requirements 优先级）
4. 列表/表单/详情与错误态、空态
5. 基础联调与环境变量

## 工作约束

- 接口路径、字段名以 `docs/api.md` 为准
- 避免在页面堆砌无交互卡片与无关营销模块
- 不把密钥写进前端源码
- UI 文案与江湖互助业务语境一致
