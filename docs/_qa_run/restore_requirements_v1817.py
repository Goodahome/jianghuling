# -*- coding: utf-8 -*-
"""Restore docs/requirements.md to pre-v1.8.18 (undo compliance-driven edits)."""
from pathlib import Path

path = Path(r"F:/Jinanghu_Ling/docs/requirements.md")
text = path.read_text(encoding="utf-8")

# 1) Header
old_header = text.split("---\n\n## 1. 产品概述", 1)[0]
new_header = """# 产品需求文档（Requirements）

> 由 **产品经理 AI（@pm）** 维护。来源：《江湖令项目立项书 V1.0》+ 关键决策确认（2026-08-05 / 2026-08-06）。

**版本**：v1.8.17  
**状态**：v1.8.16 反馈/面包屑待落地；**本版收口成果详情可达、Admin 独立成果审核、有成果禁止直接取消全额退**  
**最后更新**：2026-08-07  
**变更说明**：v1.8.17 ① **成果详情可达（C 端）**：「成果查看」须为列表并可下钻完整详情；令主看本令全部、揭榜侠看自己提交，协作中/待验收及终态只读均可看（§3 #51、§6.34.1、§9.19）。② **武林盟 Admin 独立成果审核**：侧栏独立入口 + 列表/详情 + 通过/驳回；不得仅嵌悬赏详情且看不到正文；执事堂验功详情若缺正文一并补齐（§3 #52、§6.34.2）。③ **有成果禁止直接取消全额退（P0 硬规则）**：任意已提交记录即算有成果；令主取消须进分配页（§6.5 / §6.23.3），二次确认提示「已有成果，须分配」；无成果仍可全额退（§3 #53、§6.34.3）。下一棒：**@architect → @backend → @frontend → @qa**。

"""
if "---\n\n## 1. 产品概述" not in text:
    raise SystemExit("anchor missing: ## 1")
text = new_header + "---\n\n## 1. 产品概述" + text.split("---\n\n## 1. 产品概述", 1)[1]

# 2) Overview: remove extra rows + §1.1
text = text.replace(
    "| 要解决的问题 | 租房信息真假难辨、找人帮忙成本高、善意付出难获得对等回报；需要可托管、可协作、可评价、可仲裁的悬赏机制。 |\n"
    "| 当前阶段（v1.8.18） | **邀请制内测 / 试点**：模拟银两（非真币）；充值提现对用户隐藏；域名示例 `jianghu.kssrol.com`（以实际部署为准） |\n"
    "| 合规基准文档 | `docs/江湖令_V1_上线所需资质与合规实施清单.docx`（公司主体、ICP/公安备案、规则体系、正式资金路径等） |\n"
    "\n"
    "### 1.1 与合规清单的关系（产品裁定）\n"
    "\n"
    "| 议题 | 裁定 |\n"
    "|------|------|\n"
    "| 产品形态 | 互联网平台型：发令 → 揭榜 → 执行 → 验收 → 分配 → 信用/侠义；**不变方向**（合规清单「一、统一定位」） |\n"
    "| 内测资金 | 继续 **模拟钱庄**（决策 #4 / #31）：托管/结算/发放走模拟余额；**不接真实支付**；界面显著标识非真币 |\n"
    "| 正式上线资金 | **采纳合规清单「四、统一资金方案」精神**：不建议长期「用户充值 → 平台余额 → 平台提现」自建钱包；正式对外前须评估并接入 **合规第三方支付/结算**，平台主责悬赏/验收/结算状态与记录 |\n"
    "| 资质与规则 | 正式上线前须具备营业执照、域名、ICP 备案、公安联网备案、用户协议/隐私/悬赏规则/行为规范、举报申诉与内容审核等（见 §6.35）；**内测不宣称「已合规完备」** |\n"
    "| 实名 | MVP：提供能力、**暂不作发令硬门槛**（#5）；正式上线按风险分级加强（合规清单「六」）→ 见 P1 |\n"
    "\n"
    "---\n",
    "| 要解决的问题 | 租房信息真假难辨、找人帮忙成本高、善意付出难获得对等回报；需要可托管、可协作、可评价、可仲裁的悬赏机制。 |\n"
    "\n"
    "---\n",
)

# 3) Decision rows #36 #38 #52 #53; remove #54 #55
text = text.replace(
    "| 36 | 移动端视觉适配（v1.8.4） | v1.8.3 桌面视觉落地后，须补齐 **手机窄屏**（建议 ≤768px，含约 375 宽）适配：告示板壳（瓦顶/边距/不横向溢出）、顶栏与抽屉菜单、首页品牌区与 CTA、广场工具条/防骗箴言/纸贴栅格、登录注册表单、执事堂同壳页。**不改业务逻辑**。【v1.8.18】实现侧已大面积落地；纸贴栅格：**≤768px 两列**，仅 **≤300px 单列**（含常见 360 宽双列）。交 `@qa` 窄屏回归签收。 |",
    "| 36 | 移动端视觉适配（v1.8.4） | v1.8.3 桌面视觉落地后，须补齐 **手机窄屏**（建议 ≤768px，含约 375 宽）适配：告示板壳（瓦顶/边距/不横向溢出）、顶栏与抽屉菜单、首页品牌区与 CTA、广场工具条/防骗箴言/纸贴栅格、登录注册表单、执事堂同壳页。**不改业务逻辑**；完成后交 `@qa` 窄屏回归。**待前端** |",
)
text = text.replace(
    "| 38 | 子页返回（v1.8.6 · 三端） | **侠士端、执事堂、武林盟后台**统一按导航深度分级：**一级**（各端主导航/侧栏/底栏直达）可不放页内返回；**二级及以上**须有明显「返回」并回到合理上一层（优先明确父路由，其次 `history.back`，无历史则回安全列表/工作台）。含侠士端三级页（协作会话/提交成果/完结分配）、执事堂审核详情及更深下钻、后台详情/二级路由/全屏编辑页（纯弹层关闭按钮可视作返回）。成功停留页须返回或明确 CTA。【v1.8.18】主干路径已落地；残留漏网由 `@qa` 抽查登记缺陷，不再标「整项待前端」。 |",
    "| 38 | 子页返回（v1.8.6 · 三端） | **侠士端、执事堂、武林盟后台**统一按导航深度分级：**一级**（各端主导航/侧栏/底栏直达）可不放页内返回；**二级及以上**须有明显「返回」并回到合理上一层（优先明确父路由，其次 `history.back`，无历史则回安全列表/工作台）。含侠士端三级页（协作会话/提交成果/完结分配）、执事堂审核详情及更深下钻、后台详情/二级路由/全屏编辑页（纯弹层关闭按钮可视作返回）。成功停留页须返回或明确 CTA。**待前端三端全量排查** |",
)

# Replace #52/#53/#54/#55 block with original #52/#53
import re

pat_5255 = re.compile(
    r"\| 52 \| 武林盟 Admin 独立成果审核（v1\.8\.17） \|.*?\| 55 \| 运维与体验口径（v1\.8\.18 · 已实现约束） \|.*?\n\n---",
    re.S,
)
repl_5253 = """| 52 | 武林盟 Admin 独立成果审核（v1.8.17） | 后台须有 **独立侧栏入口 + 列表页**（建议 path `/admin/submissions` 或 `/admin/submission-reviews`，由 architect 定，须进菜单种子）；列表支持待审/已审筛选、悬赏关联、提交人、时间、状态；**详情页**可看完整成果内容并执行通过/驳回（及既有改判若有）。不得仅嵌在悬赏详情且看不到正文；悬赏详情内嵌列表可保留为快捷入口，但须可下钻详情。执事堂 `/hall/submission-reviews` 若详情缺正文，一并要求可看详情。细则见 §6.34.2 |
| 53 | 有成果禁止直接取消全额退（v1.8.17 · P0 硬规则） | **重申并升级** §6.23.3：协作中/待验收，本令 **已存在任意成果提交记录**（含待审/通过/驳回版本；**不以「审核通过」为门槛**，避免令主赶在验功前取消白嫖劳动）时：令主仍可点「取消悬赏」，但 **不得**走「全额解冻退回 + 直接 CANCELLED」捷径；**必须**进入分配页按 §6.5 / §6.23.3 分完（10% 服务费 + 90% 分给有提交的侠士）；二次确认文案须提示「已有成果，须分配」；后端取消接口须按分支执行，前端不得在有成果时表现为「一键取消即退款」。无任何成果提交时仍可直接取消全额退。有成果取消 = 取消分支分配，**不是**禁取消按钮（交叉 §6.28）。细则见 §6.34.3 |

---"""
m = pat_5255.search(text)
if not m:
    raise SystemExit("failed to match #52-#55 block")
text = pat_5255.sub(repl_5253, text, count=1)

# 4) P0 section: from ### P0-A through ### P2 before ## 5
p0_old_start = text.find("### P0-A 用户端（侠士）")
p0_end = text.find("\n## 5. 用户故事")
if p0_old_start < 0 or p0_end < 0:
    raise SystemExit("P0 anchors missing")

p0_restored = r'''### P0-A 用户端（侠士）

- [ ] **邀请注册** + 验证码/账号密码登录（登录免邀请码）
- [ ] **法律条款**：注册同意《用户服务协议》《隐私政策》；页脚可打开全文（见 6.21）
- [ ] **用户反馈（v1.8.16）**：侠士端可提交；武林盟后台可处理（见 6.32）
- [ ] **面包屑样式（v1.8.16）**：侠士/执事堂文字+`/`，固定位置（见 6.33）
- [ ] **子页返回（v1.8.6 · 三端）**：侠士端 / 执事堂 / 武林盟后台，二级及更深均有页内返回（见 6.22）
- [ ] 邀请能力：生成/分享邀请（额度可配置）；**邀新成功奖励邀请人 100 两**
- [ ] 侠士资料、实名入口（非硬门槛）
- [ ] **模拟钱庄（v1.7）**：余额/流水/托管/结算；注册赠 **500 两**；单位 **两**；**MVP 暂时隐藏**充值/提现入口（能力保留）
- [ ] **站内消息**：列表 + **未读角标/红点**；已读状态同步（见 6.17）
- [ ] **租房令状（结构化模板）**：租房 / 出租 / 转租悬赏分模板发令（见 6.2、6.26）
- [ ] **赏银建议**：按难易度推荐赏银；**最低 200 两**不可提交（见 6.2.1）
- [ ] **探子清单**：发令勾选清单；成果按项提交与核验（见 6.4.1）
- [ ] **告示栏**：浏览平台告示/防骗/租房须知；标准文稿入库（见 6.14、6.27）
- [ ] **榜单列表视觉复用（v1.8.12）**：悬赏榜纸贴/木牌样式抽公共组件；我的悬赏、告示栏列表视觉对齐（见 6.30）— **仅展示层，不改接口**
- [ ] **页眉规范（v1.8.9）**：取消 subtitle；分割线统一固定长（见 6.24）
- [x] 租房悬赏榜与详情（广场路由 `/plaza`；金榜钉墙视觉见 §6.20 / §14.1.1）
- [ ] 一键揭榜（同令 1 次、每日 ≤10、耗体力）
- [ ] **退出揭榜（v1.8.8）**：协作中可单方退出；体力返还、当日次数不返还、同令不可再揭（见 6.23.5）
- [ ] 体力恢复；侠义值兑体力、兑奖品
- [ ] **协作会话（v1.8.9）**：令主与揭榜侠士双向收发可见；修复对方收不到（见 6.25）
- [ ] **悬赏全生命周期（v1.8.10）**：详情按状态展示令主/揭榜能力；终态禁会话发送与成果提交；令主可查看成果（见 6.28）— QA **Fail**（§9.14 / D-V1810-01·02）
- [ ] **成果详情可达（v1.8.17）**：成果查看=列表+详情下钻；令主看全部、揭榜侠看自己；终态只读可看（见 6.34.1）
- [ ] 成果多次提交（防刷）
- [ ] 完结全额分配（服务费 10%）；超时提醒与自动退款
- [ ] **悬赏取消细则（v1.8.8）**：有成果取消进分配页面、取消登记与声望扣减、待验收超时自动完结、取消争议工单、纠纷中可强制关闭（见 6.23）
- [ ] **有成果禁止直接取消全额退（v1.8.17 · P0）**：任意提交记录即算有成果；取消须进分配页；无成果才全额退（见 6.34.3 / 6.23.3）
- [x] **再发一令**：已取消 / 已完结 / 审核驳回的悬赏可复制重新发布（见 6.18）
- [ ] 互评 → 声望（好评率 + 完成单量）
- [ ] **等级展示与晋升进度**（见 6.10）
- [x] **英雄榜**浏览（原称英雄谱；界面展示「英雄榜」）；个人排名与上榜信息
- [ ] **武林盟主申请**入口（满足声望条件时）
- [ ] **职司申请**入口（等级达标可申请令审使/验功使等）
- [x] **执事堂**（职司工作台）：待审队列、处理、查看本人操作记录；**武侠视觉对齐侠士端**；审核列表同「我的悬赏」；可看详情；明显返回（见 6.19）
- [ ] **移动端视觉适配（v1.8.4）**：告示板/金榜/防骗箴言/登录注册/执事堂在窄屏下可用（见 6.20.6）
- [ ] 纠纷发起与进度查看

### P0-B 武林盟后台（完整管理，MVP 必做）

#### 1）工作台

- [ ] 运营概览：用户数、待审发令/成果、纠纷单、今日揭榜、资金托管汇总等

#### 2）用户管理

- [ ] 侠士列表/详情（等级、声望、侠义值、体力、完成单、好评率、邀请关系）
- [ ] **侠士详情页（v1.8.11）**：独立路由下钻；资料/资产/职司等分区；备注、解封、实名、登录日志（见 §6.29.1）
- [ ] 启用/禁用、封禁解封、备注
- [ ] 资产手工调整（侠义值/体力/模拟余额，须记审计日志）
- [ ] 实名信息查看与状态维护
- [ ] 登录日志 / 关键行为摘要

#### 3）邀请管理

- [ ] 邀请码生成、失效、配额；邀请关系查询

#### 4）悬赏与协作管理

- [ ] 悬赏令列表/详情；强制下架/关闭；状态变更
- [ ] **发令审核**（通过/驳回+原因）
- [ ] **成果审核**（通过/驳回+原因）
- [ ] **独立成果审核页（v1.8.17）**：侧栏入口 + 列表/详情/通过驳回；可看完整成果正文（见 6.34.2）
- [ ] 揭榜关系与提交记录查看
- [ ] 会话记录抽检（风控需要时）

#### 5）交易与钱庄（模拟）

- [ ] 托管/结算/退款流水；**发放类流水**（注册赠银、邀新奖励、管理员发放）
- [ ] **用户端暂时隐藏充值/提现**（配置可开关，默认关）；管理员调账/发放入口保留（审计）
- [ ] 服务费汇总；异常流水标记

#### 6）纠纷仲裁

- [ ] 仲裁工单队列；举证材料；裁决与执行
- [ ] **纠纷结构化详情（v1.8.11）**：非 JSON dump；展示举证、关联悬赏、裁决表单（见 §6.29.3）

#### 7）成长与运营配置

- [ ] **等级配置**：各级名称、晋升所需侠义值、权益说明（入口：`/admin/ops` · tab `levels`，见 §6.31）
- [ ] **武林盟主**：现任展示、申请单审批、任免/罢免（须审计）
- [ ] **职司与权限**：职司定义、申请门槛、名额、审批、授予/暂停/撤销、操作抽检与改判
- [ ] **英雄谱**规则：榜单类型、刷新策略、展示条数（入口：`/admin/ops` · tab `ranks`，见 §6.31）
- [ ] 体力/揭榜日限/提交频控/兑换汇率等 **成长参数**（入口：`/admin/ops` · tab `growth`，见 §6.31）
- [ ] **赏银建议档位**配置（最低赏银、各难度建议区间；入口：`/admin/ops` · tab `reward`，见 §6.31）
- [ ] 奖品与兑换订单管理

#### 8）内容与风控

- [ ] 举报处理；敏感操作审计日志查询
- [ ] **告示栏管理**：发布/下架/置顶/分类（规则、防骗、租房须知等）
- [ ] **用户反馈管理（v1.8.16）**：列表/详情/处理状态；侧栏入口（见 6.32）
- [ ] **探子清单**模板配置（可增删改检查项、是否必填；入口：`/admin/checklist`，见 §6.31）
- [ ] **令状字段**配置（入口：`/admin/warrant-config`，见 §6.31）
- [ ] 职司操作监控：驳回率、被改判率、异常预警

#### 9）系统管理

- [ ] 后台账号与 **管理员 RBAC**（菜单/按钮级：超级管理员、运营管理员、终裁仲裁员等）
- [ ] **管理员 / 角色 / 菜单三页（v1.8.11）**：账号 CRUD、角色权限配置、菜单树；侧栏按权限显隐（见 §6.29.2；对齐 D-003 / api.md §16.10）
- [ ] **侧栏运营入口可达与层级（v1.8.13–15）**：运营配置下仅一条「运营参数」+ 奖品/清单/令状；权限管理为目录；不压平；path 可达（见 §6.31）
- [ ] **核心权限**不可授予侠士职司（见 6.13）
- [ ] 菜单管理、字典/枚举、系统参数
- [ ] 操作日志、登录日志
- [ ] （可选但建议）定时任务状态查看：超时取消、提醒、榜单刷新

### P1

- [ ] 真实支付通道；**重新开放**用户充值/提现（取消 MVP 隐藏）
- [ ] 实名作为高额发令门槛
- [ ] 声望/等级加权推荐与更细徽章墙
- [ ] 更细租房结构化模板、举报策略增强
- [ ] VIP / 推广曝光
- [ ] 副盟主/长老席；更多职司（如调解使）
- [ ] 职司绩效自动奖惩与竞选制

### P2

- [ ] 百晓生 AI
- [ ] 同城多品类、企业悬赏、开放平台等

'''

# Keep the intro paragraph before P0-A if present
preamble_marker = "> **说明（相对 v1.2）**"
pre_p0 = text.rfind("## 4. 核心功能", 0, p0_old_start)
section4 = text[pre_p0:p0_old_start]
# Ensure section4 ends with blank line before P0
if not section4.endswith("\n\n"):
    section4 = section4.rstrip() + "\n\n"
text = text[:pre_p0] + section4 + p0_restored + text[p0_end:]

# 5) §6.29 background
text = text.replace(
    "> **背景（v1.8.18 对照现状 · 已更新）**：  \n"
    "> - 前端：**已有** `/admin/users/:id` 详情、列表解封、调账弹窗、删除=禁用；**已有** `/admin/admins|roles|menus`；侧栏按权限/菜单树；`/admin/system` 已重定向为 **审计日志** `/admin/audit-logs`。  \n"
    "> - 前端：纠纷 **结构化详情页** 已替代整包 JSON dump。  \n"
    "> - 契约：api.md §16.3 / §16.7 / §16.10 / §16.12 已齐；Admin 成果审核 path = **`/admin/submission-reviews`**。  \n"
    "> - 本小节下文部分「待确认」为历史缺口记录；**实现已覆盖主干的，以代码与 api.md 为准**，不再按旧背景阻塞研发。\n",
    "> **背景（对照现状）**：  \n"
    "> - 前端：`/admin/users` 仅列表（禁用/封禁/调账），**无详情路由**；`adminGetUser` 已有但未挂页；列表缺「解封」按钮。  \n"
    "> - 前端：`AdminLayout` 侧栏 **写死全量菜单**，无 `/admin/admins|roles|menus` 路由/页面；`SystemView` 仅为系统参数 JSON + 审计，**非 RBAC CRUD**。  \n"
    "> - 前端：`DisputesView` 详情弹层用 `<pre>{{ detail }}</pre>` **JSON dump**；终裁靠单行 prompt 拼 `KEEP|…`，无结构化表单。  \n"
    "> - 契约：api.md **§16.3 / §16.7 / §16.10** 路径与权限模型已齐；**§16.3 详情/登录日志/实名、§16.7 详情举证结构与 reallocations/punishments 项形**仍缺字段级示例 → 下文标 **待确认**，须 architect 补契约后再编码。  \n"
    "> - 后端：用户详情/备注/启停封禁解封/调账/登录日志/实名、纠纷队列/详情/裁决、管理员/角色/菜单 Controller **已有能力**；部分响应仍为 Map/JSON 字符串，以契约补齐为准。\n",
)

# 6) Remove §6.35
s635 = text.find("### 6.35 上线资质与合规实施")
s618 = text.find("### 6.18 再发一令")
if s635 < 0 or s618 < 0 or s618 < s635:
    raise SystemExit("§6.35 removal anchors failed")
text = text[:s635] + text[s618:]

# 7) Restore §6.34.2 path line
text = text.replace(
    "   - **path 已裁定**：`/admin/submission-reviews`（禁止 `/admin/submissions`）；须写入菜单种子且 RBAC 可控，与 router 一一可达。  \n",
    "   - 建议 path：`/admin/submissions` 或 `/admin/submission-reviews`（**由 architect 裁定**，须写入菜单种子且 RBAC 可控，与 router 一一可达）。  \n",
)

# 8) Restore §8
text = text.replace(
    """## 8. 非功能需求

- **性能**：支撑遵义小规模试运营；列表/详情/英雄谱流畅。
- **安全**：登录鉴权；管理员 RBAC 与侠士职司权限隔离；L0 不可下放；资金与调账防重放；审核/任免/授撤权/调账全量审计；提交限流；试点/生产 **禁用长期 `dev` profile**（禁止 Mock 验证码与开发态管理员密码重置逻辑外泄）。
- **可用性**：Web 响应式（≤768px 两列纸贴，≤300px 单列）；主路径清晰；执事堂与管理后台职责分明，且执事堂视觉不脱离侠士端江湖气质。
- **合规（内测）**：须落地 **用户服务协议 + 隐私政策**（见 §6.21、`docs/legal/`）；注册同意与页脚入口；模拟资金显著标识「非真实货币/测试账户」。
- **合规（正式上线）**：对齐 §6.35 / 合规清单——主体与备案、经营性信息服务评估、规则体系完备、分级实名、内容审核与举报申诉、商标；资金走合规支付路径，禁止未评估即开放真实充值提现。
- **可运营**：日常审核可交给职司；管理员聚焦监督、终裁与核心配置；空库须手工灌 `schema`/`data`/`patch`（不自动初始化）。
- **部署（现状）**：K8s 独立命名空间、独立 Redis、可接平台现有 MySQL；入口域名以实际配置为准（示例 `jianghu.kssrol.com`）；详见 `docs/deployment.md` / 根目录 `README.md`。
""",
    """## 8. 非功能需求

- **性能**：支撑遵义小规模试运营；列表/详情/英雄谱流畅。
- **安全**：登录鉴权；管理员 RBAC 与侠士职司权限隔离；L0 不可下放；资金与调账防重放；审核/任免/授撤权/调账全量审计；提交限流。
- **可用性**：Web 响应式；主路径清晰；执事堂与管理后台职责分明，且执事堂视觉不脱离侠士端江湖气质。
- **合规**：须落地 **用户服务协议 + 隐私政策**（见 §6.21、`docs/legal/`）；注册同意与页脚入口；模拟资金显著标识「非真实货币/测试账户」；正式对外前补齐运营主体与法务审阅（待确认项）。
- **可运营**：日常审核可交给职司；管理员聚焦监督、终裁与核心配置。
""",
)

# 9) Remove §9.20 and restore §10-12
s920 = text.find("### 9.20 v1.8.18")
s10 = text.find("## 10. 范围外")
if s920 < 0 or s10 < 0:
    raise SystemExit("§9.20 / §10 anchors failed")
# Keep content before 9.20; replace from 9.20 through end of §12 intro table start
# Find ## 11 and ## 12 and content after stage 2 line
rest_from_10 = text[s10:]
# Replace the modified §10-12 block at start of rest
old_1012 = rest_from_10
# Use known restored block for 10-12 beginning until "第二阶段" line ends
restored_1012_head = """## 10. 范围外（相对 MVP）

- 进行中修改赏银金额或随意延期（易引发资金与协作纠纷；P1 另议）
- 悬赏取消违约金 / 从托管赏银扣罚（v1.8.8 已确认：无成果取消全额退回、有成果取消进分配页面分完，均 **不设违约金**；信誉约束走取消登记与声望扣减，见 §6.23.3）
- 揭榜侠士取消悬赏（揭榜人无悬赏取消权；v1.8.8 起可 **退出揭榜** 解除自身揭榜关系，见 §6.23.5）
- 待审核中原地编辑再提交（v1.8 可用「驳回/取消 + 再发一令」替代；P1 可增强）
- 公开自助注册（无邀请开放注册）
- 真实微信支付/支付宝
- 小程序 / App
- 百晓生 AI
- 非租房品类、企业悬赏、开放平台
- 实名硬门槛、复杂帮会/动态社交
- 副盟主/长老会等多席位政治玩法（P1）
- 武林盟主自动拥有后台超管权限（明确不做）
- 将资金调账、费率、管理员账号等 L0 权限下放给侠士（明确不做）
- **C 端公开成果广场**（v1.8.17：路人浏览他人成果不做；成果详情仅令主/提交人及审核侧可见）
- **有成果取消后自动按人头均分赏银**（v1.8.17：仍由令主在分配页手动分配；非本版新做强制自动分）

---

## 11. 商业模式（MVP 落地项）

| 模式 | MVP |
|------|-----|
| 成交服务费 | **10%**（自托管赏银扣减，可分配池为 90%） |
| VIP / 企业认证 / 开放平台 | 不做 |
| 推广服务 | 不做或极简（非必须） |

---

## 12. 发展路线

| 阶段 | 目标 |
|------|------|
| MVP | 遵义 · 租房悬赏闭环 + 完整武林盟后台 + 等级/英雄谱/武林盟主 |
| 第二阶段 | 同城互助多品类 + 真实支付 |
"""

# Find where original content continues after our modified 12 table (next | or ## 13)
idx_cont = rest_from_10.find("| 第二阶段 |")
if idx_cont < 0:
    raise SystemExit("stage2 row missing")
# skip to end of that line
nl = rest_from_10.find("\n", idx_cont)
tail_after_12table = rest_from_10[nl + 1 :]
# If next lines are our added "上线准备" row, drop until blank line before ## 13 or next content
# Current modified table may have extra row 上线准备
# rebuilt: restored head ends with 第二阶段 line + newline; then original may have more rows or ---
# Drop any following table rows that were only in 1.8.18 until we hit blank + --- or ##
while tail_after_12table.startswith("| "):
    # drop 上线准备 row if present
    line, _, rem = tail_after_12table.partition("\n")
    if "上线准备" in line or "MVP / 内测" in line:
        tail_after_12table = rem
        continue
    break

text = text[:s920] + restored_1012_head + tail_after_12table

# 10) mobile row + 14.7
text = text.replace(
    "| **移动端（v1.8.4）** | 窄屏补齐壳/顶栏/首页/广场/登录注册/执事堂；纸贴 ≤768 两列、≤300 单列（见 §6.20.6、#36） | [x] 已实现，待 QA 窄屏回归 |",
    "| **移动端（v1.8.4）** | 窄屏补齐壳/顶栏/首页/广场/登录注册/执事堂（见 §6.20.6） | [ ] 待前端 |",
)
text = text.replace(
    "### 14.7 仍待确认（未进 MVP / 上线阻塞项）\n\n"
    "1. **信物徽章** 是否进下一迭代？  \n"
    "2. **堂口/地界** 做「标签」即可，还是要「加入地界」组织感？  \n"
    "3. **茶馆** 要不要，还是告示栏已够用？  \n"
    "4. **法律条款运营信息**（§6.21.5）：主体法定名称/地址、隐私联系邮箱、法务终审、管辖法院——不阻断内测展示，阻断「正式对外宣称合规完备」。  \n"
    "5. **公司主体与字号**（合规清单「二」）：拟用「XX网络科技有限公司」——正式名称待定。  \n"
    "6. **正式上线支付合作方与是否开放用户提现**（§6.35.2）——未定则不得对公网开放真充值。  \n"
    "7. **标准告示全文**是否已按 `docs/notices/standard-notices.md` 全量入库。  \n",
    "### 14.7 仍待确认（未进 MVP）\n\n"
    "1. **信物徽章** 是否进下一迭代？  \n"
    "2. **堂口/地界** 做「标签」即可，还是要「加入地界」组织感？  \n"
    "3. **茶馆** 要不要，还是告示栏已够用？  \n"
    "4. **法律条款运营信息**（§6.21.5）：主体法定名称/地址、隐私联系邮箱、法务终审、管辖法院——不阻断内测展示，阻断「正式对外宣称合规完备」。\n",
)

# Sanity checks
for bad in ["v1.8.18", "### 6.35", "### 9.20", "### 1.1 与合规", "| 54 |", "| 55 |"]:
    if bad in text:
        print("WARN still contains:", bad)

path.write_text(text, encoding="utf-8")
print("restored", path)
print("version line:", [ln for ln in text.splitlines() if ln.startswith("**版本**")][0])
print("lines", text.count("\n") + 1)
